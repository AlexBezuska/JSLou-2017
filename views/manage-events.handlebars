<h2>Manage Event

{{#if_eq state.task "create"}} > Create Mode{{/if_eq}}
{{#if_eq state.task "edit"}} > Edit Mode{{/if_eq}}
</h2>

{{#if justAdded}}
<div class="panel panel-success">
  <div class="panel-heading">
    <h4 class="panel-title">Success!</h3>
  </div>
  <div class="panel-body">
    <p>The new event on: {{editingEvent}} has been added to events.json.</p>
  </div>
</div>
{{/if}}


<div class="row">
  <div class="col-xs-6">
      {{> events}}
  </div>
  <div class="col-xs-6">
    <div class="panel panel-default">
      <div class="panel-heading">
 <strong>{{editingEvent}}</strong> Event Properties</div>
      <div class="panel-body">
        {{> form}}
      </div>
    </div>
  </div>
</div>